namespace Tetrifact.Core
{
    public enum PackageAddErrorTypes
    {
        MissingValue,
        PackageExists,
        InvalidFileCount,
        InvalidArchiveFormat,
        UnexpectedError
    }
}
