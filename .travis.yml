services:
  - docker

branches:
  only:
  - master
  - develop

jobs:
  include:
    - stage: test
      script:
      - cd tests
      - sh ./test.sh
    - stage: deploy
      on:
        tags: true
      script:
      - echo "tag"
