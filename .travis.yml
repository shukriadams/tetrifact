services:
  - docker

branches:
  only:
  - master
  - develop

jobs:
  include:
    - stage: deploy
      on:
        tags: true
      script:
      - echo "tag"
    - stage: test
      script:
      - cd tests
      - sh ./test.sh

